// import Todo from "./model";

const list = (app, Todo) => {
  var todo = new Todo();
  todo._id = 
};
const write = (app, Todo) => {};
const update = (app, Todo) => {};
const remove = (app, Todo) => {};
